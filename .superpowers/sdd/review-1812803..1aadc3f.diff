# Review package: 1812803..1aadc3f

## Commits
1aadc3f feat: Cerezo IntentClassifier ÔÇö keyword-based intent classification

## Files changed
 backend/app/services/cerezo/__init__.py   |  0  backend/app/services/cerezo/classifier.py | 30 +++++++++++++++++++++++++++  backend/tests/test_cerezo_classifier.py   | 34 +++++++++++++++++++++++++++++++  3 files changed, 64 insertions(+)

## Diff
diff --git a/backend/app/services/cerezo/__init__.py b/backend/app/services/cerezo/__init__.py new file mode 100644 index 0000000..e69de29 diff --git a/backend/app/services/cerezo/classifier.py b/backend/app/services/cerezo/classifier.py new file mode 100644 index 0000000..cbb8913 --- /dev/null +++ b/backend/app/services/cerezo/classifier.py @@ -0,0 +1,30 @@ +_KEYWORDS: dict[str, list[str]] = { +    "club_info": ["datos", "informaci├│n", "informacion", "c├│mo es", "como es", "historia", "detalles"], +    "match_result": ["gan├│", "gano", "resultado", "c├│mo qued├│", "como quedo", "├║ltimo partido", "ultimo partido", "├║ltimo cl├ísico", "ultimo clasico"], +    "head_to_head": ["historial", "c├│mo le fue", "como le fue", "contra", "versus", "enfrentaron", "cl├ísicos", "clasicos"], +    "table_position": ["tabla", "posiciones", "lidera", "posici├│n", "posicion", "campeonato"], +    "prediction": ["gana", "pr├│ximo", "proximo", "predicci├│n", "prediccion", "pron├│stico", "pronostico", "va a ganar"], +    "top_scorer": ["goleador", "artillero", "m├íximo", "maximo", "goleadores"], +    "greeting": ["hola", "buenas", "buen", "c├│mo est├ís", "como estas", "buen d├¡a", "buen dia", "qu├® tal", "que tal"], +} + + +class CerezoIntentClassifier: + +    @staticmethod +    async def classify(message: str) -> dict: +        text_lower = message.lower() +        scores: dict[str, int] = {} +        for intent, keywords in _KEYWORDS.items(): +            count = sum(1 for kw in keywords if kw in text_lower) +            if count > 0: +                scores[intent] = count + +        if not scores: +            return {"intent": "unknown", "confidence": 0.0, "entities": {}} + +        best_intent = max(scores, key=scores.get) +        best_score = scores[best_intent] +        confidence = min(round(0.5 + (best_score - 1) * 0.2, 4), 0.95) + +        return {"intent": best_intent, "confidence": confidence, "entities": {}} diff --git a/backend/tests/test_cerezo_classifier.py b/backend/tests/test_cerezo_classifier.py new file mode 100644 index 0000000..8a7f91f --- /dev/null +++ b/backend/tests/test_cerezo_classifier.py @@ -0,0 +1,34 @@ +import pytest +from backend.app.services.cerezo.classifier import CerezoIntentClassifier + + +@pytest.mark.asyncio +async def test_classify_greeting(): +    result = await CerezoIntentClassifier.classify("Hola Cerezo") +    assert result["intent"] == "greeting" +    assert result["confidence"] >= 0.5 + + +@pytest.mark.asyncio +async def test_classify_club_info(): +    result = await CerezoIntentClassifier.classify("Datos de Olimpia") +    assert result["intent"] == "club_info" +    assert result["confidence"] >= 0.5 + + +@pytest.mark.asyncio +async def test_classify_table_position(): +    result = await CerezoIntentClassifier.classify("C├│mo viene la tabla") +    assert result["intent"] == "table_position" + + +@pytest.mark.asyncio +async def test_classify_prediction(): +    result = await CerezoIntentClassifier.classify("Qui├®n gana el pr├│ximo partido") +    assert result["intent"] == "prediction" + + +@pytest.mark.asyncio +async def test_classify_unknown(): +    result = await CerezoIntentClassifier.classify("xyzzy flurbo garplax") +    assert result["intent"] == "unknown"
